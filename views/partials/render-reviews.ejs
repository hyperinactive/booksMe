<hr />

  <div class="container">
    <div class="row">
      <% reviews.forEach(review=>{ %>
      <div class="col-md-4">
        <h2><%=review.book.title%></h2>
        <p><%=review.rev%></p>
        <p><%=review.user%></p>
      </div>
      <% }); %>
    </div>

    <hr />